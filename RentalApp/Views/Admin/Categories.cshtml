@model List<RentalApp.Models.EquipmentCategory>
@{
    ViewData["Title"] = "Kategorie sprzętu";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="CreateCategory" class="btn btn-success">Dodaj kategorię</a>
</p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Nazwa</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var cat in Model)
        {
            <tr>
                <td>@cat.Name</td>
                <td>
                    <a asp-action="EditCategory" asp-route-id="@cat.Id" class="btn btn-sm btn-warning">Edytuj</a>
                    <form asp-action="DeleteCategory" method="post" style="display:inline">
                        <input type="hidden" name="id" value="@cat.Id" />
                        <button type="submit" class="btn btn-sm btn-danger">Usuń</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>